spring:
  jpa:
    hibernate.ddl-auto: validate
flyway.enabled: true
flyway.locations: classpath:/db/migration
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
